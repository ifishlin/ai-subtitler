# Python environment
.venv/
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Pipeline artifacts (all regenerable by main.py); --output can name any of these
output/
output_*/

# Downloaded and intermediate media
work/*.mp4
work/*.wav
work/*.webm
work/*.m4a
work/*.info.json

# Reviewed sidecars are kept in version control on purpose:
#   work/*.corrections.json
#   work/*.extra_segments.json

# Subtitle editor cache (proxy video, waveform peaks, session state)
editor_cache/

# Run logs and downloaded B-roll
*.log
work/broll_*

# Cut-out icons are produced from img/ by src/cutout.py
img_cut/
