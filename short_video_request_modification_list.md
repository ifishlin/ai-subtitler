# 短影音 Request 修改清單

來源：`川普要把安大略湖改名-request.txt`

這份清單的目標不是重寫整份 request，而是找出目前規格中可能讓 LLM 誤解、產生不一致 JSON、或被後端 gate 擋下來的地方，並提供最小修改方式。

---

## 先說結論

目前最值得優先修改的是：

1. 明確定義「有旁白」與「無旁白」，並標明本次是哪一種模式。
2. 最後 checklist 要把 `stock` 納入合法鏡頭類型。
3. 統一 `bg` / `bg_search` 放置位置。
4. 正式 JSON 範例直接加入 `seen: true`。
5. 把 `outro` 補進合法 card kind。
6. 把殘留的「九十秒」改成「六十秒」。
7. 不要再用「通常四到六段」描述影片段數，改用實際秒數限制。

其中第 1 點目前 **不一定會造成實際錯誤**，因為 prompt 後面反覆強調 `20–25` 句，LLM 很容易推斷本次是「無旁白版」。但這仍然是靠模型自己推理，而不是規格明確寫出來。

---

# 1. 有旁白 12–15 句 vs 無旁白 20–25 句

## 原文位置

### `## 數量`

> 句數　有旁白 12–15 句　無旁白 20–25 句

以及：

> 有旁白算唸的速度（每秒 4.5 字），無旁白算讀的速度（每秒 4.6 字）。

### `## 最後檢查`

> `lines` 的長度是 20–25 句嗎？數一遍。

## 問題

如果本次是「有旁白版」，前面的規格要求 12–15 句，但最後 checklist 又要求 20–25 句，兩者無法同時成立。

不過目前 LLM 通常仍會得到正確結果，因為 prompt 對 `20–25` 句的訊號非常強：

> 句數不是建議，是規格。

> 不足會被門擋下來整份退回。

> 秒數和比例確實不用自己算；句數要數。

> `lines` 的長度是 20–25 句嗎？數一遍。

所以模型很容易自行推論：

> 「這次一定是無旁白版。」

因此這一點目前屬於 **規格上的歧義，不一定是實際 bug**。

## 另外一個更重要的問題：目前沒有真正定義「有旁白」和「無旁白」

文件只有描述兩者的：

- 句數不同
- 閱讀／唸稿速度不同
- 無旁白每句至少 1.9 秒

但沒有明確定義：

- 有旁白 = 有人聲把 `say` 唸出來？
- 無旁白 = `say` 只顯示成字幕／畫面文字，不播放人聲？
- `say` 在無旁白版是否仍然全部出現在畫面？
- 無旁白是否因此「一 line = 一頁／一鏡頭」？

目前只能從其他規則推測，沒有正式定義。

## 建議修改

如果 pipeline 本來就是固定做無旁白，可以直接簡化成：

```text
## 模式

本次製作無旁白版。

無旁白版：
- `say` 不會被人聲唸出，觀眾直接閱讀畫面上的文字。
- 一句 `say` 對應一個鏡頭。
- 共 20–25 句。
- 讀速以每秒 4.6 字估算。
- 每句至少停留 1.9 秒。
- 總長目標 60 秒。
```

然後把：

```text
句數      有旁白 12–15 句　無旁白 20–25 句
```

改成：

```text
句數      20–25 句
```

如果未來同一份 prompt 真的要同時支援兩種模式，則改成：

```text
## 模式

有旁白版：
- `say` 會被旁白逐句唸出。
- 12–15 句。
- 以每秒 4.5 字估算。

無旁白版：
- `say` 不播放人聲，由觀眾直接閱讀。
- 20–25 句。
- 以每秒 4.6 字估算。
- 每句至少停留 1.9 秒。

題目資料會明確指定本次使用哪一種模式。
不得自行選擇。
```

---

# 2. `stock` 被最後 checklist 排除

## 原文位置

### `# 每一句配什麼畫面 → ## 四種鏡頭`

文件明確定義四種：

```text
用影片
用靜圖
用卡片
用情境影片
```

情境影片的 JSON 又明確使用：

```json
{"show": "情境：夕陽下的高壓電塔", "stock": "V7"}
```

但前面的 `## 最後檢查` 寫：

> 每一句都有 `pic` 或 `clip` 或 `card` 嗎？只寫 `show` 不算。

少了 `stock`。

## 風險

模型按照 visual 規則合法使用：

```json
"stock": "V7"
```

但按照 checklist 又會覺得這句不合格。

## 建議修改

把：

```text
每一句都有 `pic` 或 `clip` 或 `card` 嗎？只寫 `show` 不算。
```

改成：

```text
每一句必須有且只有一種鏡頭：
`pic`、`clip`、`card`、`stock`。
只寫 `show` 不算。
```

「有且只有一種」也可以避免模型同一 line 同時塞：

```json
{"pic": "P13", "card": {...}}
```

---

# 3. `bg` 的位置互相矛盾

## 原文位置

### `## 卡片也可以有背景照片`

範例寫：

```json
{"show": "自製：改名清單", "bg": "P16",
 "card": {"kind": "chain", "title": "2025 年的改名清單",
          "points": ["墨西哥灣→美國灣", "迪納利山→麥金利山"], "tone": "cool"}}
```

這代表 `bg` 在 line 層。

但後面立刻又寫：

> `bg` 寫在 `card` 物件裡面

而 `bg_search` 的範例也是：

```json
{"card": {"kind": "word", "title": "…", "bg_search": "aerial coastline sunset"}}
```

也就是放在 `card` 裡。

## 風險

LLM 會看到兩套 schema：

```json
{"bg": "P16", "card": {...}}
```

或：

```json
{"card": {"bg": "P16", ...}}
```

如果 gate 只接受其中一套，就可能整份退回。

## 建議修改

統一成全部放在 `card` 裡：

```json
{
  "show": "自製：改名清單",
  "seen": true,
  "card": {
    "kind": "chain",
    "title": "2025 年的改名清單",
    "points": ["墨西哥灣→美國灣", "迪納利山→麥金利山"],
    "tone": "cool",
    "bg": "P16"
  }
}
```

規則直接寫：

```text
`bg` 和 `bg_search` 都屬於 `card` 物件。

可以：
"card": {"kind": "word", "bg": "P16"}

可以：
"card": {"kind": "word", "bg_search": "aerial coastline sunset"}

不可以：
"bg": "P16", "card": {...}
```

---

# 4. 正式輸出 JSON 範例沒有 `seen`

## 原文位置

### `## 輸出格式`

照片範例：

```json
{"say": "台詞", "role": "角色", "show": "這張圖是什麼",
 "pic": "P18", "from": "出處"}
```

影片範例：

```json
{"say": "台詞", "role": "角色", "show": "影片：這段在演什麼",
 "clip": "C3", "from": "出處"}
```

但 visual 後面明確要求：

> 照片和影片段落都要，漏了那一句會被門擋下來、整份退回。

範例則是：

```json
{"show": "太空站拍的安大略湖", "pic": "P13", "seen": true}
{"show": "影片：川普在橢圓形辦公室簽署", "clip": "C8", "seen": true}
```

## 風險

LLM 很容易模仿最前面的正式 canonical JSON 範例，而忽略後面數百行之後才出現的 `seen` 規則。

## 建議修改

直接修改正式輸出格式：

```json
{
  "view": "一句話講完你的觀點",
  "tone": "詼諧 或 克制",
  "lines": [
    {
      "say": "台詞",
      "role": "角色",
      "show": "自製：畫面說明",
      "card": {"kind": "word", "title": "…"},
      "from": "出處 或 觀點"
    },
    {
      "say": "台詞",
      "role": "角色",
      "show": "這張圖是什麼",
      "pic": "P18",
      "seen": true,
      "from": "出處"
    },
    {
      "say": "台詞",
      "role": "角色",
      "show": "影片：這段在演什麼",
      "clip": "C3",
      "seen": true,
      "from": "出處"
    },
    {
      "say": "台詞",
      "role": "角色",
      "show": "情境：城市空景",
      "stock": "V4",
      "from": "觀點"
    }
  ]
}
```

---

# 5. `outro` 沒有列在合法 card kind

## 原文位置

### `## 卡片要指定怎麼畫`

字彙表只有：

```text
word
number
ring
swap
bars
split
chain
queue
stack
clock
```

但後面的：

### `## 最後一句一定是結尾頁`

要求：

```json
"card": {"kind": "outro", ...}
```

## 風險

文件一邊暗示合法 `kind` 只有字彙表中的種類，一邊又要求最後一定使用 `outro`。

## 建議修改

直接在字彙表加：

```text
outro   最後一句專用結尾頁
        title / points[] / under / tone
```

並寫清楚：

```text
`outro` 只能出現在最後一個 line。
最後一個 line 必須是 `outro`。
```

---

# 6. 60 秒規格裡殘留「九十秒」

## 原文位置

### `## 最後一句一定是結尾頁`

原文：

> `points` 是摘要，3–4 條。看完九十秒的人拿到的是……

但整份任務開頭是：

> 從底下的素材寫出一份 60 秒的短影音文案。

## 風險

主要是舊版文字殘留。

不一定會讓 LLM 真的生成 90 秒，但會增加不必要的時間規格衝突。

## 建議修改

直接改成：

```text
`points` 是摘要，3–4 條。看完六十秒的人拿到的是……
```

或更乾脆：

```text
`points` 是摘要，3–4 條。看完整支影片的人拿到的是……
```

第二種更不容易在未來影片長度改動時再次留下舊數字。

---

# 7. 「通常四到六段影片」不適合寫死

## 原文位置

### `## 比例`

先規定：

```text
外部素材      佔總長 50%（下限 50%、上限 65%）
其中影片段落   至少佔外部素材的 50%
```

因此 60 秒影片：

- 外部素材：30–39 秒
- 真實影片段落至少約 15 秒

後面又寫：

> 段數自己算：拿 15 秒去除素材清單上那些段落的長度……通常會是四到六段。

## 問題

本題素材 C1–C40 很多單段本身就是約 5–10 秒。

因此：

- 2–3 段有時就可能超過 15 秒
- 4 段如果平均 7 秒，已約 28 秒
- 6 段如果平均 7 秒，已約 42 秒

而外部素材總上限只有 39 秒。

所以「四到六段」在不同題目中不一定成立。

## 建議修改

不要給固定段數，直接要求秒數：

```text
影片段落不要用固定段數判斷。

60 秒影片：
- 外部素材總長必須介於 30–39 秒。
- 其中真實影片 `clip` 的總長至少 15 秒。
- 照片 `pic` 用來補足外部素材比例。
- `stock` 和 card 背景不算外部素材。
```

如果仍然想保留提示，可以寫：

```text
需要幾段 `clip` 取決於素材清單中每段的實際長度，
不要預設一定是四到六段。
```

---

# 建議新增：明確說明 20 張也可以做到 60 秒

目前無旁白規格是：

```text
20–25 句
每秒 4.6 字
每句至少 1.9 秒
每句最多 13 個中文字
60 秒
```

因為一句對應一個鏡頭，所以無旁白版約等於：

```text
20–25 個鏡頭
```

並不代表一定要做到 24 或 25 張。

例如 20 個鏡頭：

```text
前 19 個鏡頭平均約 2.8 秒
最後 outro 約 6.5 秒

19 × 2.8 + 6.5 ≈ 59.7 秒
```

因此 **20 張仍然可以達到約 60 秒**；只要多數句子接近 12–13 字，或部分 clip 本身停留較久即可。

如果想讓模型明確知道這件事，可以加入：

```text
20–25 句都合法，不需要為了湊 60 秒刻意增加句數。
句數較少時，可以讓每句接近字數上限，或依影片段落實際長度延長停留。
```

---

# 建議新增：事實與觀點不要混在同一句

目前規則是：

```text
陳述事實 → 一定要有出處
自己的判斷 → from 寫「觀點」
```

但如果一句同時包含兩者，例如：

```text
五十州都不買單，這改名只是自嗨。
```

前半是事實，後半是判斷。

這時 `from` 很難決定應該是來源還是「觀點」。

建議補：

```text
一句只能主要承擔一種功能：事實或觀點。

不要在同一句加入新的事實與新的判斷。
需要兩者時拆成兩句：
第一句寫事實並標來源；
下一句寫判斷並標「觀點」。
```

---

# 建議新增：把事實也編號

目前 `pic`、`clip` 已經有：

```text
P13
C8
```

但 `from` 仍然需要模型自由輸入：

```text
PBS NewsHour 第 20 秒
CNN 第 70 秒
BBC News〈...〉
```

素材很多時容易出現：

- 寫錯來源名稱
- 寫錯秒數
- 把同一家不同影片混在一起
- 自己縮寫名稱

可以把事實也編號：

```text
[F01] 川普簽署行政命令……
[F02] 命令要求聯邦機構……
[F03] 美國五十州全部搜尋「安大略湖」更多……
```

輸出改成：

```json
"from": "F03"
```

程式再自己把 `F03` 展開成真正來源。

這會同時：

- 降低 hallucination
- 減少 token
- 更容易驗證
- 更容易讓 gate 做 exact match

---

# 建議的最終規格整理方式

整份 request 現在已經很完整，下一步最值得做的不是繼續增加更多說明，而是把格式統一。

建議最後只保留三個 authoritative 區塊：

## A. 任務模式

```text
本次：無旁白
20–25 句
60 秒
每句最多 13 字
每句至少 1.9 秒
```

## B. 唯一合法 JSON Schema

一次完整示範：

```text
pic / clip / card / stock
seen
bg
outro
cover
```

不要在後文再次出現不同結構的 JSON 範例。

## C. 最後 Gate Checklist

只檢查真正會退件的硬條件。

例如：

```text
- [ ] 20–25 lines
- [ ] 起承轉合順序正確
- [ ] 轉在前 34%
- [ ] 每句 <= 13 字
- [ ] 每句有且只有 pic / clip / card / stock 其中一種
- [ ] pic / clip / bg 使用外部素材時 seen=true
- [ ] 外部素材 50–65%
- [ ] clip >= 外部素材的 50%
- [ ] 最後一張為 outro
- [ ] 有 cover.tagline
```

---

# 優先修改順序

## 第一優先：可能直接造成 schema / gate 錯誤

1. `stock` 加進 checklist
2. `bg` 位置統一
3. 正式 JSON 範例加入 `seen`
4. `outro` 加入合法 kind

## 第二優先：消除時間與模式歧義

5. 明確定義有旁白／無旁白，以及本次模式
6. 九十秒改成六十秒／整支影片
7. 四到六段改成實際秒數限制

## 第三優先：讓模型更穩、更省 token

8. 事實編號 F1、F2、F3……
9. 禁止一句混合新事實與新觀點
10. 把所有硬規格收斂成一份 authoritative schema + checklist
