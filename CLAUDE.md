# 這個專案怎麼改

## 發現的錯，要變成檢查

修完一個錯，問一句：**這條規則有沒有辦法變成門？**

有辦法就去做，不要只修這一次的症狀。三種寫法，照效果排：

```
1  變成門        measure() 的一個欄位，不做就過不去
2  變成流程的一步  東西直接擺在眼前，不是叮嚀「記得去拿」
3  變成自動顯示    每次都產出，逼迫「看」發生
```

**為什麼**：這個專案裡沒被違反過的規則，全部是程式會算的
（13 字、90 秒、每句要有來源、實拍比例）。被違反的，全部只是寫在文件裡的
（選圖前要看過、出處要上畫面、一半實拍要會動）——而那三條都是我自己寫進
`assets/prompts/` 又讀過至少兩次的。

而且**視覺的錯不會報錯**。照片被推出畫面、字被切掉、credit 沒燒上去，
沒有例外、沒有 stack trace、沒有測試失敗，程式跑得完完全全成功。
它們只在有人看的時候才存在。

已經這樣做過的：

| 發現的錯 | 變成什麼 |
|---|---|
| 三段引用都沒燒上 credit | `uncredited` 門，而且 join 搬進 `measure()` |
| 結尾連續六張一樣的卡 | `samey` 門 |
| 「沒有這一欄」被切掉兩邊 | `fits()` 量寬度決定字級，不查字數表 |
| 憑搜尋詞挑圖，沒打開看 | `unchecked` 門 + `core/brief.py` 把候選擺到眼前 |
| 畫格平均分布盲抽 | 用字幕挑時間點，沒字幕的不當畫格來源 |
| 讀錯自己截的圖 | `build()` 每次自動產接觸表，取樣由文案時間軸決定 |
| 改了畫法卻沿用舊鏡頭 | 鏡頭檔名含**畫它的程式的指紋**，改程式就換名字 |
| 先刪舊圖再抓新圖，抓到一半失敗 | `topic.replace_images()`：**新的收齊才刪舊的** |

## 兩種規則，成功率差很多

```
A 規範「寫出來的東西」   13 字、結論落在誰身上   一邊寫一邊就在遵守，機率高
B 規範「要做的動作」     打開圖來看、先排接觸表   離開寫字去做另一件事，機率低
```

**這個專案違反過的全部是 B。** 送 prompt 對 A 有效，對 B 幾乎沒用。
B 只有兩條路：變成流程的一步，或變成過不去的門。

## 數字只留一份

門檻在 `assets/rules.json`，外觀在 `assets/theme.json`。
程式從那裡讀，**prompt 也從那裡填**——md 裡寫 `{caption.per_row}`，
`core/rules.py` 的 `fill()` 在送出前代入。

引用一個數字的文字會過期，指名一個數字的不會。
兩份不同步的邏輯，就是網頁把三分之一實拍的文案報成「自製 100%」的原因。

## 不能檢查的怎麼辦

**把品質要求，翻譯成一個必須宣告的結構，然後檢查那個宣告。**

「要有起承轉合」檢查不了；「每句要標 `role`、四種都要有、轉在前三分之一」
檢查得了。範圍從「整篇結構對不對」縮成「這一個轉夠不夠有力」——
剩下那一點交給 `core/critic.py` 的第二次呼叫，和最後看片的人。

## 破壞性的步驟永遠放最後

先刪再收、先清空再重建——**中間任何一個失敗都會把原本的東西一起帶走**。
一次重收素材刪掉了四十七張照片，然後在第三個搜尋詞失敗，那個題目的 JSON
就指著一堆不存在的檔案。

順序要倒過來：**新的收齊 → 寫進紀錄 → 才掃掉沒人指著的舊檔**。
失敗只損失時間，不損失素材。`core/topic.py` 的 `replace_images()` 就是這件事。

同一個形狀在這個專案出現過三次：批次編輯最後一步失敗、從舊字串重複 replace、
先刪後收。**都是「破壞已經發生，而產出還沒發生」。**

## 一個編輯上的坑

改多處文字時，**每次都要接續上一次的結果**：

```python
t = swap(t, old1, new1)
t = swap(t, old2, new2)     # 接續，不是從原文重來
p.write_text(t)             # 最後寫一次
```

從原文重新 `replace` 再逐次寫檔，後面的寫入會蓋掉前面的。
這個 session 因此掉了三節 prompt，而且沒有任何錯誤訊息。
