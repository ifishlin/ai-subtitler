# 短影音文案生成規格

## 任務

根據提供的素材，寫出一份 **{length.limit_seconds} 秒**的短影音文案。

你的首要任務不是摘要新聞，而是：

1. 找出素材沒有直接說出的**核心觀點**
2. 用「{structure.roles}」建立完整論證
3. 讓結論落到題目指定的受眾身上
4. 嚴格輸出指定 JSON 格式

全部使用 **{language.name}**。

台詞、卡片文字、角色名稱都必須遵守該語言規則；不得混用不符合指定語言的文字。

這份文件負責**文案、觀點與敘事結構**。  
畫面選擇、圖片、影片、卡片與視覺規格遵守 `visual.md`。  
最後必須把文案與視覺決策整合成同一份 JSON。

---

## 優先順序

規則衝突時，依下列順序判斷：

1. **不捏造事實**：事實必須有素材支持
2. **核心觀點成立**：必須是素材沒有直接寫出的判斷
3. **敘事成立**：起承轉合完整，尤其「轉」必須真正改變理解
4. **結論落到指定受眾**
5. **台詞自然、口語、有節奏**
6. **滿足句數、字數、JSON 與其他格式規格**

---

# 硬規格

## 語言

全部使用 **{language.name}**。

若指定為繁體中文（台灣用語）：

- 台詞使用自然台灣口語
- 卡片上的字使用繁體中文
- 角色名稱使用繁體中文
- 不得出現簡體中文

---

## 長度

- 總長：**{length.limit_seconds} 秒**
- 有旁白：**{length.lines} 句**
- 無旁白：**{length.lines_silent} 句**
- 有旁白速度：每秒約 **{pace.spoken_per_second} 字**
- 無旁白閱讀速度：每秒約 **{pace.read_per_second} 字**
- 無旁白每句至少停留 **{pace.least_seconds} 秒**
- 觀點句：約佔全部句子的 **一半**

**句數是硬限制。輸出前必須自行確認 `lines` 的數量符合本題模式。**

---

## 每一句

每一行至少包含：

```text
say     台詞
role    {structure.roles} 其中一個
show    畫面描述
from    事實來源，或「觀點」
```

並依 `visual.md` 加上：

```text
pic / clip / card
```

### 台詞規則

- `say` 最多 **{caption.per_row} 個中文字**
- 要口語，必須是人真的念得出口的句子
- 不要寫成新聞稿或書面摘要
- 數字要念出來  
  - 「四十兆」，不要寫「40兆」
  - 「百分之三點四」，不要寫「3.4%」

### 出處規則

- 陳述事實的句子一定要有出處
- 出處必須能指回提供的素材
- 自己的判斷標 `from: "觀點"`
- `from: "觀點"` 不能夾帶素材沒有支持的新事實

---

## 結構

每一句都要標 `role`。

順序必須是：

```text
起 → 承 → 轉 → 合
```

定義：

```text
起   先給觀眾以為自己知道的版本
承   把這個版本推到底，讓觀眾更確信
轉   推翻原本理解，必須在前段出現
合   把新的理解落到指定受眾身上
```

硬規格：

- 四種 role 都必須出現
- 順序不能顛倒
- 第一個「轉」必須出現在前 **{structure.turn_before:pct}**

品質判準：

> **轉之後，觀眾對前面同一批事實的理解必須改變。**

只是增加新資訊，不算「轉」。

---

# 輸出格式

只輸出 JSON，不要輸出任何說明、Markdown 或額外文字。

圖片和影片只使用素材清單中的編號，不要寫檔名。

```json
{
  "view": "一句話講完你的核心觀點",
  "tone": "詼諧 或 克制",
  "lines": [
    {
      "say": "台詞",
      "role": "角色",
      "show": "自製：畫面說明",
      "card": {
        "kind": "word",
        "title": "…"
      },
      "from": "出處 或 觀點"
    },
    {
      "say": "台詞",
      "role": "角色",
      "show": "這張圖是什麼",
      "pic": "P18",
      "from": "出處"
    },
    {
      "say": "台詞",
      "role": "角色",
      "show": "影片：這段在演什麼",
      "clip": "C3",
      "from": "出處"
    }
  ]
}
```

`P18`、`C3` 是素材清單中的編號，直接照抄。

- 不要寫檔名
- `clip` 只寫編號
- 不要自行填影片起訖秒數
- 起訖與長度由程式從候選清單填入

---

# 先讀，再想，最後才寫

素材裡可能有立場不同的媒體。

先找出：

- 他們彼此矛盾的地方
- 同一件事被不同方式解讀的地方
- 大家都在談 A，卻沒有人問 B 的地方

**觀點通常長在這些地方。**

不要從「最重要的事實」開始寫。

最重要的事實通常所有媒體都已經講過，觀眾也可能已經看過。

你要找的是：

> **他們都沒有直接說出的那句話。**

---

## 觀點的標準

核心觀點必須：

1. 一句話說得完
2. 有素材支撐
3. 不是單純重述事實
4. 不是把兩家媒體的說法排在一起
5. 素材裡沒有任何一篇直接這樣寫過

例如：

```text
不算觀點   「國債破了四十兆」
            → 這是事實

不算觀點   「A 說漲、B 說沒漲」
            → 這是彙整

算觀點     「兩邊都對，因為算的不是同一件事」
            → 這是判斷
```

常見的觀點來源：

```text
互相衝突的數據
→ 可能算的不是同一件事

同一事件被不同方式解讀
→ 差別本身可能就是故事

所有人都在問 A
→ 真正重要的可能是沒人問的 B
```

---

# 指定受眾與結論

題目會提供一欄：

```text
說給誰聽
```

**直接使用這個受眾。**

不要自行換成：

- 更大的群體
- 更泛的「所有人」
- 另一個你覺得更合理的族群

最後一句一定要回答這個受眾：

> **「所以關我什麼事？」**

不是說「這件事很重要」，而是說：

> **「這件事會在哪裡碰到你？」**

可能的接觸點包括：

- 錢
- 時間
- 健康
- 安全
- 工作
- 帳戶餘額
- 日常生活中的具體選擇

不要寫：

```text
「這是全球經濟的重大警訊。」
「各國政府必須正視這個問題。」
```

這些沒有回答觀眾「跟我有什麼關係」。

好的結論會像：

```text
「你不會收到帳單，你會收到分期版。」
「加油站那本帳不會被審計，而那一本是你在付。」
```

判準：

> 把最後一句念給一個平常不看新聞的人聽，他要能立刻知道這件事跟自己有什麼關係。

如果最後只能硬扯到生活，代表前面的論證還沒有真正落地。

---

# 語氣

預設可以詼諧，但題材決定語氣。

以下題材使用**克制**語氣：

- 死亡
- 疾病
- 災難
- 戰爭傷亡
- 其他明顯涉及真實受害者的題材

詼諧要來自：

- 比喻
- 反差
- 荒謬性
- 邏輯本身

不要拿受害者、病人或罹難者開玩笑。

例如：

```text
好   「左口袋欠右口袋八萬塊，然後宣布自己破產。」
壞   拿罹難人數或病人開玩笑
```

---

# 寫每一句時的原則

## `say`

- 只講一件事
- 優先短句
- 口語
- 能直接念出來
- 不要只是把新聞標題切短

## `role`

只能是：

```text
{structure.roles}
```

必須符合整體起承轉合的位置。

## `from`

如果是素材中的事實：

```text
from = 具體來源
```

如果是你的推論：

```text
from = "觀點"
```

不要用「觀點」逃避來源要求。

## `show`

這一行播放時畫面上出現什麼。

詳細規則遵守 `visual.md`。

---

# 輸出前自檢

輸出 JSON 前，逐項確認：

1. `lines` 數量符合本題模式的硬規格
2. `view` 是素材沒有直接寫出的判斷，而不是新聞摘要
3. 四種 role 都有，而且順序是「起 → 承 → 轉 → 合」
4. 第一個「轉」出現在前 **{structure.turn_before:pct}**
5. 「轉」真的改變了觀眾對前面事實的理解
6. 每一個事實句都有可追溯的來源
7. `from: "觀點"` 沒有夾帶新事實
8. 語氣符合題材
9. 最後一句直接落到題目指定的受眾
10. 每一行都有 `pic`、`clip` 或 `card`，不能只有 `show`
11. 最終輸出只有合法 JSON
