# 短影音視覺生成規格

## 任務

每一句台詞對應一個鏡頭。

你要為每一行決定：

1. 使用哪一種畫面
2. 畫面內容是什麼
3. 如果使用外部素材，選哪一個素材編號
4. 如果使用自製卡片，選哪一種 `card.kind` 並填完整必要欄位

文案、觀點、起承轉合與來源規則遵守 `script.md`。

---

## 優先順序

規則衝突時，依下列順序判斷：

1. **畫面必須和台詞一致**
2. **外部素材只能使用 caption / transcript 明確支持的內容**
3. **事情正在發生時優先使用影片**
4. **講判斷、推論或結論時優先使用自製卡片**
5. **避免畫面形式過度重複**
6. **滿足素材比例、JSON schema 與 renderer 規格**

---

# 三種鏡頭

```text
用影片   事情正在發生
         現場、群眾、機具運轉、人物正在說話或行動

用靜圖   要看清楚一個東西
         人物、地點、帳單、電表、文件、圖表、物件

用卡片   在講道理
         判斷、比較、提問、邏輯關係、摘要、結論
```

判斷原則：

> **有動作就優先影片；要辨認東西就用靜圖；在解釋邏輯就用卡片。**

不要用靜圖代替明明適合影片的「事件正在發生」畫面。

---

# 外部素材比例

外部素材包含：

- `pic`
- `clip`

規格：

- 建議比例：約 **{borrowed.aim:pct}**
- 硬上限：**{borrowed.most:pct}**
- 影片時間至少佔外部素材時間的 **{borrowed.clip_least:pct}**
- 每三分之一的影片時間內，至少出現一次外部素材

以 **{length.limit_seconds} 秒**影片為例：

- 外部素材約 22 秒
- 其中影片至少約 11 秒
- 通常使用 2–3 段影片
- 每段約 4–6 秒

外部素材不是越多越好。  
能用自製卡片清楚表達的推論，不要硬塞外部畫面。

---

# 使用影片

影片行格式：

```json
{
  "show": "影片：這段在演什麼",
  "clip": "C3",
  "seen": true
}
```

規則：

- `clip` 只寫素材清單中的編號，例如 `C3`
- 不要寫檔名
- 不要自行填 `start` / `end`
- 不要自行修改候選片段時間
- 起訖與長度由程式從候選清單填入
- 剪出的影片一律靜音

只有當候選素材的 caption / transcript 足以支持這個畫面描述時，才能使用。

不要根據沒有提供的畫面資訊自行猜測。

---

# 使用靜圖

靜圖行格式：

```json
{
  "show": "這張圖實際呈現的內容",
  "pic": "P13",
  "seen": true
}
```

規則：

- `pic` 只寫素材清單中的編號，例如 `P13`
- `show` 必須描述 caption 明確支持的內容
- 不要猜它是「新聞畫格」「資料照」或其他未提供的類型
- 如果 caption 不足以確認畫面內容，就不要使用該素材

---

# `seen`

使用任何外部素材時都必須：

```json
"seen": true
```

適用於：

- `pic`
- `clip`

`seen: true` 的意思是：

> **提供的 caption / transcript 足以確認這個素材可以配這一句。**

如果素材描述不足以確認，不要硬配；換另一個素材或改用自製卡片。

---

# 自製卡片

講判斷、比較、邏輯、問題或結論時，優先使用 `card`。

不能只寫：

```text
show = "自製：一條線分岔成兩條"
```

還必須提供可直接 render 的 `card` 結構。

例如：

```json
{
  "show": "自製：一條線分岔成兩條",
  "card": {
    "kind": "split",
    "title": "他們算的不是同一件事",
    "branches": [
      "發電\n夠不夠",
      "電線\n夠不夠"
    ],
    "tone": "light"
  }
}
```

---

# `card.kind`

可用種類：

```text
word    一句話佔滿畫面
        title

number  強調一個數字
        title / value / under / colour / ghost

ring    圈出一個關鍵詞
        title / value / under

swap    劃掉舊的，換成新的
        title / was / now

bars    比較兩到三個量
        title / rows

split   一個概念分成兩邊
        title / branches[2]

chain   同一條邏輯線上的數個節點
        title / points[] / under

queue   一排等待中的人或項目
        title / count / under

stack   一份逐項列出的清單
        title / items[]

clock   一段時間或年數
        title / value / part / under

outro   最後結尾頁
        points / title / under / tone
```

選擇原則：

- 一句核心判斷 → `word`
- 一個重要數字 → `number`
- 強調一個詞 → `ring`
- 前後名稱或狀態改變 → `swap`
- 數量比較 → `bars`
- 兩條不同邏輯 → `split`
- 因果或步驟 → `chain`
- 排隊、等待、塞車 → `queue`
- 多個項目 → `stack`
- 年數、時間比例 → `clock`

不要用只有問號、沒有資訊內容的卡片。

---

# 共通欄位

常用共通欄位：

```text
note    出處，畫在畫面頂端
ghost   背景放大的一至兩個字
tone    畫面語氣
```

---

# 顏色

`colour` 與 `bars.rows` 中的顏色欄位只能：

1. 留空
2. 使用 `#RRGGBB` 色碼，例如 `#ff7a4d`

不要使用：

```text
red
warn
ok
green
danger
```

等文字代號。

留空通常是預設選擇，renderer 會依該段 `tone` 使用一致的顏色。

---

# tone 跟著論證轉

```text
cool    鋪陳
        觀眾以為自己知道的版本

light   翻轉
        讓「事情變了」在視覺上也被看見

warm    落地與結論
        把新的理解帶回受眾身上
```

`tone` 不是照個人喜好隨機選。

它應該配合 `script.md` 的起承轉合。

---

# 避免視覺形式重複

硬規格：

```text
同一種 card.kind   最多連續 {cards.same_kind_run} 張
同一個 tone        最多連續 {cards.same_tone_run} 張
```

另外避免：

- 同一個事實用不同卡片重複講
- 連續大量 `word`
- 每一句都使用相同構圖
- 明明是推論句卻硬配外部照片
- 只為了湊外部素材比例而使用不相關畫面

---

# 外部畫面中的文字與數字

新聞影片或圖片本身可能已經帶有：

- 台標
- 下標
- 數字
- 圖表
- 標題

如果候選素材的 caption / transcript **明確指出**畫面上有文字或數字，避免讓相鄰卡片出現看起來互相衝突的資訊。

如果兩個不同數字其實代表不同時間點，可以把差異直接變成內容，例如：

```text
先報二百七十三人
後來增加到近六百人
```

如果素材資料沒有提供畫面文字資訊：

> **不要自行猜測畫面上寫了什麼。**

---

# 最後一句一定是結尾頁

最後一行必須使用：

```json
"card": {
  "kind": "outro"
}
```

格式：

```json
{
  "say": "整件事是這樣的。",
  "role": "合",
  "show": "自製：結尾頁",
  "seconds": {ending.seconds},
  "card": {
    "kind": "outro",
    "tone": "cool",
    "points": [
      "摘要一",
      "摘要二",
      "摘要三"
    ],
    "title": "一句觀眾可以帶走的話",
    "under": "題目一句話"
  }
}
```

規格：

- `points`：**{ending.points} 條**
- `points` 是支撐結論的核心摘要
- `title` 是一句觀眾可以記住、轉述給別人的話
- `under` 放題目
- `tone` 建議回到片頭 tone，形成視覺上的收尾

不要輸出：

- 頻道 logo
- 訂閱提示
- 右下角頻道標記

這些由程式自行加入。

---

# 常見錯誤

避免以下情況：

```text
同一個事實重複用不同卡片呈現
只有問號、沒有資訊的卡片
有明確數字卻不用適合的數字視覺
純推論句硬配外部照片
show 描述超出素材 caption 能確認的內容
標籤與素材實際描述不一致
為湊比例使用不相關素材
連續太多相同 card.kind
```

---

# 輸出前自檢

每一行完成後，確認：

1. 這句最適合 `clip`、`pic` 還是 `card`
2. 如果使用外部素材，caption / transcript 是否真的支持這個畫面
3. 所有 `pic` / `clip` 是否都有 `"seen": true`
4. 所有素材都只使用 `Pxx` / `Cxx` 編號
5. `card.kind` 與欄位是否符合 renderer 規格
6. `colour` 是否為空或合法 `#RRGGBB`
7. 外部素材比例沒有超過硬上限
8. 影片比例達到最低要求
9. `card.kind` 與 `tone` 沒有連續超過限制
10. 最後一行是 `outro`
11. `show` 沒有描述素材資料無法確認的內容
